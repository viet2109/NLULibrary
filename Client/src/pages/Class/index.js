import React from 'react';
import PropTypes from 'prop-types';

Class.propTypes = {
    
};

function Class(props) {
    return (
        <div>
            
        </div>
    );
}

export default Class;